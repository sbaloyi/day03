void ft_sort_integer_table(int *tab, int size);

void swap(int *a, int *b);

void swap(int *a, int *b) {

	int temp = a*;
	*a = *b;
	*b = temp;
}

void ft_sort_integer_table(int *tab, int size) {
	
	int counter = 0;
	int counter1;

	while (counter < size) {
		counter1 = counter;
		while (j < size) {
			if(tab[counter] >= tab[j]) {
				swap(&tab[i] >= tab[j]);
			}
			++counter1;
		}
		++counter;
	}
}

